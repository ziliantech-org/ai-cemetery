# Database: SQLite, auto-created at data/cemetery.db, no config needed.

# SMTP (optional): Configure to send verification emails.
# If not set, verification codes are printed to console (dev mode).
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
