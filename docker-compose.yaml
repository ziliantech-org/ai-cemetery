services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - app-data:/app/data
    env_file:
      - path: .env
        required: false
    restart: unless-stopped

volumes:
  app-data:
